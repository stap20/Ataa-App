import { StyleSheet } from "react-native";
import { Theme } from "@theme";

export default profileEditModalStyle = () => {
  return StyleSheet.create({
    contentContainer: {
      flex: 1,
      backgroundColor: "white",
    },
  });
};
